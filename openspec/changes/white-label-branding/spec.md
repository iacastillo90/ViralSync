# Specification — White Label Branding
## Requirements
- `GET /api/v1/tenants/{tenant_id}/branding` DEBE retornar la configuración de marca blanca.
- `PUT /api/v1/tenants/{tenant_id}/branding` DEBE actualizar `logo_url`, `primary_color` y `agency_name`.
