name: ViralSync Enterprise CI/CD Pipeline

on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main ]

jobs:
  python:
    runs-on: ubuntu-latest
    env:
      AGENCY_ENV: dev
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install uv
        uses: astral-sh/setup-uv@v3
      - name: Install dependencies and run tests
        env:
          AGENCY_ENV: dev
          AGENCY_ENV=dev: dev
        run: |
          cd agency
          uv pip install -r requirements.lock
          uvx ruff check backend agents workers knowledge gateway
          uvx pip-audit -r requirements.lock
          pytest --cov=backend --cov-fail-under=50 tests/unit



  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build frontend
        run: |
          npm ci
          npm run build
          npm audit

  docker-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hadolint check
        run: |
          hadolint Dockerfile

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Gitleaks check
        run: |
          gitleaks detect
